# LicenseChain Python SDK Roadmap

## Current Focus

- Keep API base defaults aligned with `https://api.licensechain.app/v1`.
- Preserve `verify_with_details` and JWT/JWKS helper compatibility.
- Maintain conformance with shared license verification contract.

## Next Operational Tasks

- Re-run contract checks when Core API verify payloads evolve.
- Keep docs references synchronized with docs and Dashboard.
- Track releases and API-surface changes in `CHANGELOG.md`.
